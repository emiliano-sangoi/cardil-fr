{{ form_start(form) }}
<div class="row mb-3">
    <div class="col-12">
        {{ form_row(form.nom) }}
    </div>
</div>

<div class="row">
    <div class="col-12 col-md-6">
        {{ form_row(form.etat) }}
    </div>
    <div class="col-12 col-md-6">
        {{ form_row(form.ordre) }}
    </div>

</div>

{{ form_end(form) }}
