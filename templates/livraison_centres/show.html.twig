{% extends 'base-admin.html.twig' %}

{% block stylesheets %}
    {{ parent() }}

    <style>
        #map {
            height: 600px;
        }
    </style>
{% endblock %}

{% block main_content %}
    <div class="row">

        <div class="col-12 col-md-8">
            <div id="map"></div>
        </div>

        <div class="col-12 col-md-4">
            <div class="h-100">
                {% include 'livraison_centres/_form.html.twig' %}

                <div class="row mt-4 mt-lg-5">
                    <div class="col-12 col-lg-6">
                        <a href="{{ path('app_livraison_centres_index', { id : fournisseur.id }) }}"
                           class="btn btn-outline-secondary">
                            <i class="fa fa-arrow-left"></i>&nbsp;{{ "Back"|trans }}
                        </a>
                    </div>
                    <div class="col-12 col-lg-6 text-lg-right   ">
                        <a class="btn btn-outline-danger" href="{{ path('app_livraison_centres_edit', {'id': livraison_centre.id}) }}">
                            <i class="fa fa-pencil"></i>&nbsp;{{ "Edit"|trans  }}
                        </a>
                        {{ include('livraison_centres/_delete_form.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}

 {% block javascripts %}
     {{ parent() }}

     {% include 'livraison_centres/_gmap-show.html.twig' %}

 {% endblock %}
