<form method="post" action="{{ path('app_livraison_centres_delete', {'id': livraison_centre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ livraison_centre.id) }}">
    <button class="btn">Delete</button>
</form>
