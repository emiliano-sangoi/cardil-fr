{% form_theme form 'bootstrap_4_layout.html.twig' %}

{{ form_start(form) }}
    {{ form_widget(form) }}

{% block form_actions %}
    <button class="btn">{{ button_label|default('Save') }}</button>
{% endblock %}

{{ form_end(form) }}
