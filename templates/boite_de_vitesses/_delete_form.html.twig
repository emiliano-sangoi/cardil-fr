<form method="post" action="{{ path('app_boite_de_vitesses_delete', {'id': boite_de_vitesse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ boite_de_vitesse.id) }}">
    <button class="btn">Delete</button>
</form>
