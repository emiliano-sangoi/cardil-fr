{% extends 'base-admin.html.twig' %}

{% block main_content %}
    <div class="row">
        <div class="col-12 col-md-6 col-lg-5">

            {% include 'boite_de_vitesses/_form.html.twig' %}

            <div class="row mt-4 mt-lg-5">
                <div class="col-12 col-lg-6">
                    <a href="{{ path('app_boite_de_vitesses_edit', {'id': boite_de_vitesse.id}) }}" class="btn btn-primary">
                        <i class="fa fa-pencil"></i>&nbsp;{{ "actions.edit"|trans }}
                    </a>
                    <a href="{{ path('app_boite_de_vitesses_index') }}" class="btn btn-outline-secondary">
                        <i class="fa fa-arrow-left"></i>&nbsp;{{ "actions.back"|trans }}
                    </a>
                </div>
                <div class="col-12 col-lg-6 text-lg-right">
                    {{ include('boite_de_vitesses/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
{% endblock %}

